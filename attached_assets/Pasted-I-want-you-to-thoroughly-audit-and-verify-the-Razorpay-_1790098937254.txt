I want you to thoroughly audit and verify the Razorpay payment integration in this project and make any necessary fixes.

### Current Setup

* Razorpay is currently configured in **Test Mode**.
* The `.env` file is already provided and contains the updated **Razorpay Test Key ID and Key Secret**.
* **Do not replace, expose, or hardcode the Razorpay credentials.** Read them from the existing `.env` configuration.
* Authentication is handled using **Firebase Auth**.
* The project already has a **question database**, so inspect and understand the existing question database/schema before making any database changes.
* The project is running on **Replit**, and I want any required development database tables to be created in the **Replit development database**.

### What I want you to do

#### 1. Audit Razorpay Integration

Go through the complete payment flow from frontend to backend and verify:

* Razorpay SDK/script initialization
* Test-mode Key ID usage
* Order creation
* Amount calculation
* Currency
* Payment checkout configuration
* Payment success handling
* Payment failure handling
* Razorpay payment verification/signature verification
* Backend API endpoints related to payments
* Webhook handling, if implemented
* Database updates after successful payment
* Handling of cancelled/failed/pending payments
* Duplicate payment prevention
* Refresh/retry behavior
* Proper error handling and user-facing messages
* Whether the frontend and backend are accidentally using different Razorpay credentials
* Whether any old/live Razorpay credentials are still referenced anywhere in the codebase

Search the entire project for Razorpay-related configuration and make sure there is a single consistent configuration source.

#### 2. Verify Test Mode

Since I am currently using Razorpay **Test Mode**:

* Ensure the integration is using the test Key ID and Secret from `.env`.
* Do not switch anything to Live Mode.
* Do not ask me to provide credentials that already exist in `.env`.
* Verify that the frontend receives only the Key ID and that the secret remains server-side.
* Check whether the current checkout configuration is compatible with Razorpay Test Mode.
* Do not assume that a missing UPI/QR option is necessarily a code bug; verify the actual Razorpay test-mode behavior and configuration first.

#### 3. Database Audit

Inspect the existing database schema and application code before creating anything.

I already have:

* Firebase Authentication
* Existing user/student-related data
* An existing question database

Determine whether Razorpay requires additional tables or columns.

If database changes are genuinely required:

* Create the necessary tables in the **Replit development database**.
* Use the project's existing ORM/database architecture and conventions.
* Do NOT create duplicate tables if equivalent tables already exist.
* Do NOT modify or delete existing production data.
* Keep development/test data separate from production data.
* Add appropriate indexes, foreign keys, timestamps, and constraints where required.
* Make the schema compatible with the existing Firebase Auth flow.

For payment records, consider whether we need to store information such as:

* Internal payment ID
* Firebase user/student ID
* Razorpay order ID
* Razorpay payment ID
* Razorpay signature
* Amount
* Currency
* Payment status
* Payment method
* Failure reason
* Created/updated timestamps
* Metadata/reference information needed to reconcile payments

Only add fields that are actually required by the current application.

#### 4. Check Existing Question Database

Before making database changes, inspect the existing question database and understand:

* Its schema
* How questions are created/read/updated
* How users are associated with questions, tests, attempts, or results
* Whether any existing tables can be reused

Do not create a second question database or duplicate existing functionality.

#### 5. End-to-End Testing

After making the required fixes, test the complete flow in development:

`User → Firebase Auth → Payment Page → Create Razorpay Order → Razorpay Test Checkout → Payment → Signature Verification → Database Update → Payment Success/Failure State`

Test at least:

* Successful payment
* Failed payment
* Cancelled checkout
* Pending/retry scenario
* Page refresh after payment
* Duplicate payment attempt
* Invalid/missing payment data
* Invalid signature
* Server/API error
* User who is not authenticated

Make sure the UI correctly reflects the actual payment status without requiring the user to manually refresh the page.

#### 6. Security Review

Check specifically for:

* Razorpay secret exposed in frontend code
* Secrets committed to Git
* Hardcoded credentials
* Missing signature verification
* Trusting frontend payment status without backend verification
* Duplicate payment processing
* Unauthorized access to payment APIs
* Firebase user ID validation
* Amount manipulation from the frontend

Fix any security issues you find.

### Important Rules

* First inspect the existing codebase and database. Do not blindly create new files/tables.
* Preserve existing functionality that is already working.
* Do not replace Firebase Auth.
* Do not replace the existing question database.
* Do not change Razorpay from Test Mode to Live Mode.
* Do not expose or print `.env` secrets in logs.
* Do not hardcode credentials.
* If a database table is required, create it in the **Replit development database** using the project's existing database/ORM setup.
* If migrations are required, create/apply them correctly.
* Do not delete existing data or tables unless absolutely necessary and explicitly justified.
* Keep the implementation production-ready even though we are testing with Razorpay Test Mode.

### Final Report

After completing the work, give me a concise report containing:

1. Razorpay issues found
2. Fixes made
3. Database tables/columns created or modified
4. Tests performed and their results
5. Any remaining issues
6. Exact files changed
7. Any environment variables required
8. Whether the complete Razorpay Test Mode flow is now working end-to-end

Do not just tell me what could be wrong. Inspect the actual project, make the required changes, and verify them.
